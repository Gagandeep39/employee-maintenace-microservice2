import { GradeTypePipe } from './grade-type.pipe';

describe('GradeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GradeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
