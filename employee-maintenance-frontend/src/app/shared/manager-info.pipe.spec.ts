import { ManagerInfoPipe } from './manager-info.pipe';

describe('ManagerInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ManagerInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
